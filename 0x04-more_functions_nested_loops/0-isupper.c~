#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: int charachter
 * Return: 0
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (0);
	}

	else
	{
		return (0);
	}
}
