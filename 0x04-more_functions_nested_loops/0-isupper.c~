#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: int charachter
 * Return: 1 if it is uppercase, 0 if not
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (0);
	}

	else
	{
		return (0);
	}

}
