#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: returns result
 */

int mul(int a, int b)
{
	int result;
	result = a * b;

	return (result);
}
