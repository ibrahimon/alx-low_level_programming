#include <stdio.h>

/**
 * main - main block
 * Descreption: printing alphabet in loxercase then in upper case
 * Return: 0 (succeed)
 */

int main(void)

{
	char x = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	c = 'A';
	
	while (c <= 'Z')
	{
		putchar(c);
		c++;
	}


	putchar('\n);
	return (0);
}
