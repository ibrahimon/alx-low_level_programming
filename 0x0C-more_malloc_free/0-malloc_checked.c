#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

/**
 * malloc_checked - allocates memory
 * @b: argument
 * Return: void
 */

void *malloc_checked(unsigned int b)
{
	void *ptr;
	ptr = malloc(b);
	if (p == NULL)
	{
		exit(98);
	}

	return (p);
}


