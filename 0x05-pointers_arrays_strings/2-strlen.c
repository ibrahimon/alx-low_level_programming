#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the lens of a string
 * @s: the string
 * Return: string length
 */

int _strlen(char *s)
{
	int u = 0;
	while (s[u] = '\0')
	{
		u++;
	}

	return (u);
}
