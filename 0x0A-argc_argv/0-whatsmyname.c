#include "main.h"
#include <stdio.h>

/**
 * main - main
 * @argc: the number of arguments
 * @argv: the array of arrguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[1]);
	return (0);
}
